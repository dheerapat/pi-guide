{
  "name": "pi-guide",
  "version": "1.1.0",
  "description": "pi-guide — custom guideline injection for pi — append your own instructions to every LLM turn",
  "keywords": [
    "pi-package"
  ],
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./extensions/guide/index.ts"
    ]
  }
}
